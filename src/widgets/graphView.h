#pragma once

#include "widgets.h"

// the thing that displays all the graphs
class GraphView : public Widget {
public:
	GraphView(const Size& SIZ=Size());
	virtual ~GraphView();

private:
	
};
